bun --hot run src/app.ts

\c effect_pg_dev

ALTER DATABASE effect_pg_dev SET timezone TO 'UTC';

# set up test_db

In psql terminal

CREATE DATABASE effect_pg_test WITH
  OWNER = postgres
  ENCODING = 'UTF8'
  TEMPLATE = template0;


ALTER DATABASE effect_pg_test SET timezone TO 'UTC';